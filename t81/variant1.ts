t81(document.querySelector('.Experiment.T81'));

document.addEventListener('cover.ready T81', (event) => {
  t81(event.currentTarget);
});

function t81(element) {
  element.style.display = 'none';
}
